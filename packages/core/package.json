{
  "name": "core",
  "type": "module",
  "private": true,
  "description": "Adhese SDK Core",
  "main": "./src/index.ts",
  "files": [
    "LICENSE",
    "README.md",
    "dist"
  ],
  "scripts": {
    "dev": "vitest --silent",
    "test": "vitest --run --coverage --silent",
    "build": "vite build",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "clean": "rm -rf dist & rm -rf coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@adhese/sdk-devtools": "^0.1.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1"
  }
}
