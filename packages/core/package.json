{
  "name": "core",
  "type": "module",
  "private": true,
  "description": "Adhese SDK Core",
  "main": "./src/index.ts",
  "files": [
    "LICENSE",
    "README.md",
    "dist"
  ],
  "scripts": {
    "dev": "vitest",
    "test": "vitest --run --coverage --silent",
    "build": "vite build",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "clean": "rm -rf lib & rm -rf coverage",
    "typecheck": "tsc --noEmit"
  }
}
