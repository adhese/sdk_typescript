{
  "$schema": "https://turbo.build/schema.json",
  "experimentalUI": true,
  "pipeline": {
    "test": {
      "outputs": ["coverage/**"],
      "dependsOn": []
    },
    "lint": {},
    "lint:fix": {},
    "typecheck": {},
    "build": {
      "outputs": ["dist/**", "lib/**"],
      "dependsOn": ["^build"]
    },
    "docs#build": {
      "outputs": ["dist/**"],
      "dependsOn": ["@adhese/sdk#build"]
    },
    "prepareRelease": {
      "outputs": ["dist/**", "lib/**"],
      "dependsOn": ["^prepareRelease"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "clean": {
      "cache": false,
      "outputMode": "errors-only"
    }
  }
}
