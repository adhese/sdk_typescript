{
  "name": "react-example",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "lint": "eslint . --ignore-pattern public",
    "lint:fix": "eslint --fix . --ignore-pattern public",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@adhese/sdk": "^1.11.7",
    "@adhese/sdk-devtools": "^1.0.2",
    "@adhese/sdk-react": "^1.11.7",
    "@adhese/sdk-stack-slots": "^1.1.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
