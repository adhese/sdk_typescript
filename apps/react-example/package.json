{
  "name": "react-example",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "lint": "eslint . --ignore-pattern public",
    "lint:fix": "eslint --fix . --ignore-pattern public",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@adhese/sdk": "^1.0.1",
    "@adhese/sdk-devtools": "^1.0.0",
    "@adhese/sdk-react": "^1.0.0",
    "@adhese/sdk-stack-slots": "^1.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
