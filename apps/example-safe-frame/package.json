{
  "name": "app-example-safeframe",
  "type": "module",
  "private": true,
  "license": "GPL-3.0",
  "scripts": {
    "dev": "vite --host --port 5174",
    "build": "vite build",
    "lint": "eslint . --ignore-pattern public",
    "lint:fix": "eslint --fix . --ignore-pattern public",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@adhese/sdk": "^1.6.2",
    "@adhese/sdk-devtools": "^1.0.1",
    "@adhese/sdk-safe-frame": "^1.0.0"
  }
}
