{
  "name": "app-example",
  "type": "module",
  "private": true,
  "license": "GPL-3.0",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "lint": "eslint . --ignore-pattern public",
    "lint:fix": "eslint --fix . --ignore-pattern public",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@adhese/sdk": "^0.20.1",
    "@adhese/sdk-devtools": "^0.11.6",
    "@adhese/sdk-stack-slots": "^0.3.6"
  }
}
